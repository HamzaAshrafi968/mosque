# AGENTS.md

## Project overview
Laravel 12 (PHP 8.2+) multi-mosque (multi-tenant) Islamic school management app: students, teachers, classes/sections, subjects, schedules, attendance, exams/grades, homeworks, lessons, announcements, messages, Quran review + reward points, plus a central "مدير الجوامع" (super admin) area, RBAC (roles/permissions/scopes) and the mosque/school spec in `.agents/mosque_management_implementation.md`. Blade RTL Arabic UI + REST API (Sanctum), Vite 7 + Tailwind CSS 4.

Demo accounts (seeded): super@mosque.test / password, admin@mosque.test / password, teacher@mosque.test / password.

## Key commands

```bash
composer setup          # Full bootstrap: install deps, copy .env, key:generate, migrate, npm install + build
composer dev            # Concurrent dev stack: artisan serve + queue:listen + pail (logs) + vite
composer test           # config:clear + php artisan test (uses SQLite :memory:)
php artisan migrate:fresh --seed   # fresh DB + demo data + full Quran (114 surahs)
npm run build           # Vite production build
npm run dev             # Vite dev server with HMR
```

## Architecture

### Multi-tenancy ("mosques")
- `Tenant` = mosque/جامع. Domain models use `MultiTenantTrait` → global `tenant` scope filtered by `config('app.current_tenant_id')`.
- `InitializeTenant` sets that config from `users.tenant_id`. **`users.tenant_id` is nullable** — only the global super admin has `null`.
- Super admin (role string `super_admin`, role row `roles.code = super_admin`, tenant-less) can "enter" a mosque: `session('super_admin_mosque_id')`; then `EnsureRole`/`InitializeTenant` treat them as that mosque's admin and all admin routes/controllers run scoped to that mosque.
- Bypass tenant scope with `withoutGlobalScope('tenant')` (used by super-admin cross-mosque queries).

### RBAC
- Tables: `roles` (tenant_id nullable = global), `role_user`, `permissions` (seeded catalog), `permission_role` (with `scope` = global/mosque/class/section/own).
- Catalog = `app/Support/PermissionCatalog.php` (codes `resource.action` + Arabic labels + default grant maps for `mosque_manager`/`teacher`). Add new permissions there.
- `app/Services/RoleService.php` — seeds catalog, global super-admin role, per-mosque default roles, role assignment; a User `created` hook auto-attaches default role from the legacy `users.role` string.
- `app/Services/AuthorizationService.php` — `can($user, 'resource.action', $subject?, $owns?)` implements permission → scope → mosque isolation chain. Middleware alias `permission:`.
- `EnsurePermission` now guards every admin/teacher web route and every `/api/v1/admin|teacher/*` endpoint (self-service dashboard/profile/logout/notifications stay open). It resolves the route-bound model and enforces `own` scope via owner columns (`user_id`, `awarded_by`, `created_by`, `recorded_by`, `teacher_id`, `supervisor_id`); subjects without owner columns fall back to tenant isolation. `Student.user_id` is **not** treated as an ownership column (it links the student to their own portal account) — teacher scope for student-bound routes is enforced in the controllers via `QuranScopeService::assertCanManageStudent`.
- Legacy route gating still uses the `users.role` string via `EnsureRole` (`role:admin` / `role:teacher` / `role:super_admin`).
- Permission changes (super-admin role matrix → `RoleService::syncRolePermissions`) take effect immediately: no permission cache, `scopesFor()` queries `permission_role` live. `tests/Feature/PermissionEnforcementTest.php` covers grant/revoke on web + API + matrix + sidebar.
- Per-user permission overrides (`permission_user`, migration `2026_09_13_000009`): مدير الجوامع edits one user's matrix at `/super-admin/mosques/{mosque}/users/{user}/edit` (the full user form: data + role + specialty + دوامات + matrix in one save; the standalone `.../permissions` page stays for direct links). Any mosque user: teacher/student/manager/guardian; super admins are excluded. A direct row wins over role grants — `effect=deny` revokes, an allow row replaces the role scopes; absent rows fall back to roles (`AuthorizationService::scopesFor()`). `global` is rejected for mosque-bound users. `RoleService::syncUserPermissions()` + `tests/Feature/UserPermissionOverrideTest.php`.
- Per-shift program access (`program_study_session`, migration `2026_09_13_000011`): each study session lists the programs available in it (no links = all programs). Saved from `/admin/sessions` via `POST admin.sessions.programs` (`permission:sessions.update`); schedule forms/API filter programs by the active/selected shift (`ProgramService::sessionProgramMap/availablePrograms`) and the server rejects a program not enabled for the shift (`ResolveScheduleProgramAction`). `tests/Feature/SessionProgramAccessTest.php`.
- Teacher multi-shift (`study_session_teacher`, migration `2026_09_15_000001`): a teacher can belong to several دوامات (e.g. الأول والثالث). `teachers.study_session_id` stays the primary/first choice; `Teacher::studySessions()` is the pivot source of truth and the `study_session` global scope matches primary OR pivot (`Teacher::applyStudySessionScope`). Teacher/user forms submit `study_session_ids[]` (empty = كل الدوامات), and `StudySession::teachers()` is a BelongsToMany. `tests/Feature/StudySessionsTest.php`.
- Per-shift classrooms/sections (`classrooms.study_session_id`, migration `2026_09_15_000002`): each دوام has its own صفوف وشعب. `Classroom` uses the `study_session` global scope but an unbound classroom (صف مشترك) stays visible in every shift; sections of a bound classroom always inherit its shift (`Section` saving hook), and binding/moving a classroom cascades its sections + students + schedules (`App\Actions\Admin\Classroom\SyncClassroomShiftAction`; bulk assign type `classrooms`). Schedule forms/API inherit the classroom/section shift and reject a mismatched `study_session_id` (`ResolveScheduleProgramAction::assertClassroomSession`). `tests/Feature/ClassroomSessionScopeTest.php`.
- `MosqueUserController` maps mosque roles to legacy `users.role` (manager→admin, guardian→guardian, student→student, teacher/custom→teacher) and provisions the matching profile (Teacher/Guardian/Student) so portals work right after creation. Teacher defaults now include `finance.view/create/adjust/transfer` (own), backfilled to existing teacher roles by `2026_09_13_000010`. `MosqueUserController::store` detaches the legacy auto-attached role so a user keeps only the requested role.
- `Admin\UserController` keeps `role_user` pivots in sync on role change (and guards the last mosque manager); `MosqueUserController` already did. `TeacherPermissionChange`-style role edits elsewhere must do the same.
- New catalog codes `parents.*`, `quran_review.*`, `reward_points.*` are backfilled to existing system roles by migration `2026_09_13_000001_backfill_new_permission_grants.php`; `programs.*` by `2026_09_13_000006_backfill_program_permissions_and_defaults.php` (which also provisions the five default schedule programs per mosque).

### Teacher payroll / رواتب المعلمين
- Managers set `teachers.monthly_salary` (nullable decimal, migration `2026_09_15_000002`) either in the teacher create/edit form or inline at `/admin/payroll` (`Admin\PayrollController`, `permission:finance.view` to view, `finance.create` to save salary/pay).
- Work hours stay a recurring weekly schedule (`teacher_work_hours`); `TeacherWorkHour::monthlyHours()` / `hoursBetween()` expand it to monthly/period totals by counting each weekday's occurrences. `hoursBetweenFromPeriods`/`monthlyHoursFromPeriods` are the batch variants. Admin + teacher work-hours pages, teacher dashboard and profile show the monthly total with a month picker (Arabic labels via `QuranProgramSettings::monthLabel`).
- `App\Services\PayrollService` records a salary payment as a normal `payment`/`money_in` ledger row, then notifies the teacher's user via `NotificationService` ("تم إيداع دفعة لك", url `teacher.finance.index`). The accumulated-hours counter is derived, never stored: it counts from the day after the last non-reversed payment (or `hired_at`), so recording a payment resets it to zero. Reversing a payment restores it.
- The teacher finance page is deposit-only: salary / monthly hours / hours-since-last-payment / last payment cards plus a "الدفعات الواردة إليّ" list (money_in rows, reversed ones flagged). The old receive/transfer/adjust routes stay for API/back-compat but are no longer linked in the teacher UI.
- Tests: `tests/Feature/TeacherPayrollTest.php`.

### الدورات الشرعية (Sharia courses) — إنشاء مركزي ومشرفون متعددون
- المشرفون متعددون عبر pivot `sharia_course_supervisor` (migration `2026_09_17_000001`، مع ترحيل `supervisor_id` القديم ثم حذفه في `2026_09_17_000002`). `ShariaCourse::supervisors()` / `Teacher::supervisedShariaCourses()` هي المصدر، و`ShariaCourseService::assertCourseAccess`/`coursesFor` تفحص الـpivot (و`EnsurePermission` يمنح `own` للأستاذ إذا كان أحد مشرفي الدورة).
- مدير الجوامع ينشئ الدورة مركزياً من `/super-admin/sharia-courses` (`SuperAdmin\ShariaCourseController` + `options` JSON endpoint): يختار الجامع (المكان) والمشرفين والطلاب الموجودين (checkbox grid مع فلتر) أو يضيف طلاباً جدداً inline، مع `tenant_id` صريح و`source=super_admin`. عند الحفظ يصل إشعار «دورة شرعية جديدة» لمديري الجامع المستهدف (`role=admin`، إرسال مباشر عبر `PortalNotification` لتفادي نطاق الجامع في `NotificationService`)، وتظهر الدورة في شاشات إدارة الجامع مع شارة «من مدير الجوامع».
- الطلاب: `sharia_course_students.student_id` (nullable FK → students، migration `2026_09_17_000003`) يربط الطالب الموجود بسجل الدورة (مع نسخ الاسم/الجنس/تاريخ الميلاد/هاتف الولي)، وتبقى الإضافة اليدوية سجلاً مستقلاً بلا `student_id`. `ShariaCourseService::syncEnrolledStudents` idempotent (فهرس unique course_id+student_id).
- حالة الحفظ لكل طالب مسجَّل (`ShariaMemorizationStatus`: لم يحفظ / حفظ أجزاء منه / حفظ النصف / حفظ كامل) مع ملاحظات ومن حدّثها ومتى؛ يعدّلها مدير الجامع أو مشرفو الدورة فقط (صلاحية `sharia_courses.memorization`، backfill `2026_09_17_000005`) من تبويب «الطلاب» في شاشات الإدارة والمعلم.
- Tests: `tests/Feature/ShariaCoursesTest.php` + `tests/Feature/ShariaCourseCentralTest.php`.

### نقاط المكافآت لكل دوام (reward points per shift)
- `reward_point_rules` (migration `2026_09_16_000009`): قاعدة لكل دوام و`rule_type` (`tasmee_pages` / `khamsa_review` / `test_pass`)، مع `pages_count` (للحفظ فقط) و`points` (فارغة/صفر = معطّل). صفحة الإعدادات `/admin/settings/rewards` (`Admin\RewardPointSettingsController`، صلاحيات `quran_settings.view/update`) + رابط Sidebar «إعدادات النقاط».
- `App\Services\RewardPointAutoService`: **حفظ الصفحات** تراكمي مع ترحيل الباقي (تسميع `new` عبر hook `saved` في `QuranRecitationSession`): الصفحات المغطاة فعلياً − `source_pages` الممنوحة سابقاً = delta، وكل ما يكمل `pages_count` يُمنح `points` (تُخزَّن لقطة `source_pages` على صف النقاط). **إتمام الخمسة** داخل `QuranKhamsaService::completeItem` (يشمل الخمسات المغلقة آلياً عند نجاح اختبار الدفعة) بحماية `source_id`. **اختبار الدفعة** عند النجاح فقط في `QuranMemorizationGatingService::recordBatchOutcome` بحماية `source_id=test`.
- `reward_points` صار فيها `study_session_id` + `source_type` + `source_id` + `source_pages`، و`awarded_by` صار nullable (منح تلقائي بلا ممثل). المنح التلقائي غير قابل للحذف من الأستاذ (`RewardPoint::isAutomatic()`)، ولا خصم رجعي عند تعديل/حذف تسميع أو تغيير القواعد.
- الواجهات: عمود الدوام + فلتر في `admin.reward-points.index` (وفلتر الدوام في المتحكم)، وعمود الدوام في `teacher.reward-points.index`، وبوابة الطالب «نقاطي» `/student/reward-points` (`Student\RewardPointController` + رابط Sidebar). `tests/Feature/RewardPointRulesTest.php`.

### Audio announcements (auto-delete after a week)
- Managers attach one audio file to an announcement (`announcements.audio_path/audio_original_name/expires_at`, migration `2026_09_15_000003`; `body` is optional when audio is present). Validation/storage lives in `app/Support/AudioUpload.php` (`mimetypes:audio/*,video/webm` — audio-only MediaRecorder WebM is often detected as `video/webm` — ≤20 MB, UUID filename with MIME-derived extension and client-MIME fallback).
- The admin announcements form also offers a WhatsApp-style **live voice recorder** (`data-voice-recorder` widget + `initVoiceRecorders()` in `resources/js/app.js`): mic button → live timer/pulsing dot → stop → preview/delete, attaching the recording to the same `audio` input via `DataTransfer`; the manual file upload stays available, and submitting mid-recording auto-stops and attaches first. Audience fan-out/notifications/expiry are unchanged.
- Audio announcements get `expires_at = now()->addWeek()` by default (`auto_delete` checkbox in the admin form, `auto_delete` boolean in `POST /api/v1/admin/announcements` to opt out). `announcements:purge-expired` (scheduled hourly in `routes/console.php`, cross-tenant via `withoutGlobalScope('tenant')`) hard-deletes expired rows; the model `deleting` hook removes the audio file. Portals + dashboards hide expired rows immediately via `Announcement::notExpired()`. `tests/Feature/AudioAnnouncementTest.php`.

### مراجعة 5 — الخمسات (khamsa reviews)
- كل جزء = ٤ خمسات × ٥ صفحات؛ `App\Support\QuranJuzMap` يحمل خريطة صفحات الأجزاء الثلاثين والخمسة الأخيرة تأخذ الباقي (جزء ١: 16–21، جزء ٦: 117–120، جزء ٣٠: 597–604). الميزة لا تعتمد على `quran_ayahs.juz` (لكنه صُحّح أيضاً عبر migration `2026_09_15_000007` + `database/data/quran_pages.json` + `fetch-quran-pages.php`).
- لا تُفتح خمسات جزء إلا بوجود سجل في `student_juz_memorizations`: يدوي من شبكة «الأجزاء المحفوظة» في نموذج الطالب (`memorized_juz_numbers[]` + `memorized_juz_numbers_present`)، أو تعبئة من `memorized_juz` (intake)، أو تلقائياً عند تغطية تسميع «جديد» كل صفحات الجزء (hook `saved` في `QuranRecitationSession`).
- `quran_khamsa_reviews` (رأس: طالب/أستاذ/دوام/تاريخ/حالة pending|completed|cancelled، `StudySessionScopedTrait` للفلترة بالدوام النشط) + `quran_khamsa_review_items` (juz/khamsa/from_page/to_page/status/result/completed_by/quran_review_session_id). `App\Services\QuranKhamsaService` يفرض القفل وتطابق الدوام ومنع تكرار الخمسة قيد المراجعة، والرأس يُغلق تلقائياً عند اكتمال عناصره.
- الشاشات: `/admin/quran/khamsa` و`/teacher/quran/khamsa` (تخصيص/عرض/إنهاء/إلغاء + إدارة الأجزاء المحفوظة) و`/student/quran-khamsa` (عرض فقط). الصلاحيات `quran_khamsa.view/create/update/complete` + `quran.memorization.manage` (mosque للمدير، own للأستاذ؛ backfill `2026_09_15_000006`). `tests/Feature/KhamsaReviewTest.php` + `tests/Feature/QuranJuzMapTest.php`.
- **دُمجت واجهياً** في مركز «دفعات الحفظ»: صفحات القوائم `admin.quran.khamsa.index` و`teacher.quran.khamsa.index` و`student.quran-khamsa` تعيد التوجيه إلى المركز/الملف القرآني، وبقيت صفحات `show/create` كإجراءات تفصيلية مرتبطة من المركز.
- **«مراجعة الخمسات مع تسجيل الأخطاء» (المربعات + التحديد المتتالي)**: شاشة «مراجعة 5» تعرض الخمسات كمربعات (`quran.khamsa.review-details`)، والأستاذ/المدير يحدد خمسات **متتالية** (بلا فجوات — يفرضه JS و`QuranKhamsaService::assertContiguousSelection`) ثم يفتح القرآن على نطاقها الكامل ويسجّل الأخطاء كلمة بكلمة بنفس واجهة التسميع (`x-quran-review-viewer`). الحفظ (`admin|teacher.quran.khamsa.review.store` عبر `App\Actions\Quran\StartKhamsaReviewSessionAction`) ينشئ جلسة «استماع مع المعلم» واحدة (`CreatePageQuranReviewSessionAction` بحد `QuranKhamsaService::MAX_REVIEW_PAGES = 30`، والحالات غير المعلَّمة = صحيحة)، ويُنهي كل الخمسات المحددة مربوطةً بها مع نتيجة مشتقة من الإتقان (`QuranTasmeeResult::fromMastery`) ومزامنة خطة الاستماع وإغلاق الرأس والدفعة (`QuranKhamsaService::completeItems`). الطالب يرى المربعات للعرض فقط. `tests/Feature/KhamsaReviewSessionTest.php`.

### خطة الاستماع والاختبار (quran listening plans)
- الأستاذ/المدير يبني خطة من عناصر مرتبة (`quran_listening_plans` + `quran_listening_plan_items`، migration `2026_09_16_000001`): عنصر «جديد» بنطاق صفحات داخل حدود الجزء، أو عنصر «مراجعة 5» (خمسة كاملة من جزء محفوظ) — `type`/`khamsa` + ربط `khamsa_review_item_id` (migration `2026_09_16_000003`). `app/Rules/PageWithinJuz` + `QuranListeningService::normalizeItems` يرفضان أي صفحة خارج نطاق الجزء (مثال: الجزء ١ = ١–٢١، الجزء ٦ = ١٠٢–١٢٠، الجزء ٣٠ = ٥٨٢–٦٠٤) وكذلك التكرار أو انقلاب النطاق.
- دمج «مراجعة 5»: خطة فيها عناصر مراجعة تُنشئ تلقائياً رأس `quran_khamsa_reviews` مرتبطاً (`listening_plan_id`/`khamsa_review_id`) وتظهر خمساته في شاشات «مراجعة 5». نجاح عنصر المراجعة في اختبار الخطة يُنهي الخمسة المرتبطة، وإنهاء الخمسة من شاشة «مراجعة 5» يسجّل استماع عنصر الخطة (مزامنة اتجاهين، `QuranKhamsaService::syncListeningPlanItem`). إلغاء الخطة يلغي المراجعة المرتبطة.
- نموذج البناء يعرض الأجزاء الثلاثين مع علامة «محفوظ» (من `student_juz_memorizations`) وزر «توليد تلقائي من الأجزاء المحفوظة» (١٠ أجزاء محفوظة = ١٠ عناصر بنطاقاتها الكاملة)، وخمسات كل جزء المفتوحة وغير المخصّصة (`BuildListeningPlanItemsAction` للتحقق والتسطيح).
- الفتح بالدفعات (`gate_size` عناصر مفتوحة معاً حتى ١٠): لا تُفتح دفعة جديدة حتى تنجح كل العناصر المفتوحة (`QuranListeningService::recomputeGating`)؛ العنصر الراسب يصبح `needs_repeat` ويُعاد استماعه ثم اختباره قبل فتح ما بعده.
- الاختبار رأس محاولة (`quran_listening_tests`) + نتيجة كل عنصر (`quran_listening_test_items`: ناجح/يحتاج إعادة + لقطة الصفحات + ربط اختياري بجلسة «الاستماع مع المعلم»). يسجّله الأستاذ/المدير من `/admin/quran/listening/{plan}` و`/teacher/quran/listening/{plan}`، ويمكن ربط جلسة «الاستماع مع المعلم» أيضاً عند تسجيل الاستماع (`quran_review_session_id` على العنصر). الطالب يرى خطته ويشغّل التلاوة ويسجّل «تم الاستماع» من `/student/quran-listening`.
- التلاوة من CDN خارجي لكل آية (`config/quran_audio.php` + `QuranAudioService`: افتراضياً everyayah/Alafasy، صيغة `SSSAAA.mp3`، عدة قرّاء)، مع مشغّل JS أصلي (تشغيل متسلسل تلقائي، اختيار القارئ، وتتبّع اختياري لثواني التشغيل). العناصر المقفلة لا يُسلَّم لها رابط التلاوة. **الاستماع للطالب فقط**: جدول عناصر الخطة بكامله («العنصر والصفحات/الحالة/المحاولات/الاستماع / التشغيل» مع المشغّل وزر «تشغيل التلاوة» و«تم الاستماع») يظهر في صفحات الطالب فقط (`showPlayer`/`canListen` في `quran.listening.plan-details` → `$showListeningTable`)، وصفحات الأستاذ/المدير تعرض التقدّم ونموذج الاختبار وسجل الاختبارات فقط (مع بقاء مسارات `items.audio/progress/listen` للتوافق).
- الصلاحيات `quran_listening.view/create/update/listen/test` (mosque للمدير، own للأستاذ، وview/listen own للطالب) + backfill `2026_09_16_000002`؛ إشعارات الطالب عند الإنشاء/النتيجة/الإتمام وسجل تدقيق `quran_listening.*`.
- `tests/Feature/QuranListeningPlanTest.php` (التحقق + الفتح بالدفعات + دمج الخمسات والمزامنة + العزل والصلاحيات) + `tests/Feature/QuranListeningAudioTest.php` (قائمة التشغيل + بوابة الطالب).
- **دُمجت واجهياً** في مركز «دفعات الحفظ»: `admin.quran.listening.index` و`teacher.quran.listening.index` و`student.quran-listening.index` تعيد التوجيه إلى المركز/الملف القرآني، وبقيت صفحة `show` (المشغّل والاختبار) كإجراء تفصيلي، والمشغّل نفسه مضمَّن داخل المركز.

### دفعات الحفظ — دورة جزأين (memorization batches) — المركز الموحّد للقرآن
- **الواجهات الأربع مدموجة في مركز واحد**: «دفعات الحفظ» (`/admin/quran/batches`، `/teacher/quran/batches`) و«ملفي القرآني» (`/student/quran`) يعرضان الدفعة الحالية ودورتها كاملة: سجل «التسميع مع المعلم» (استماع تفصيلي فقط في المركز، والسجل الموحّد الكامل في «ملفي القرآني») + مراجعة 5 + خطة الاستماع (المشغّل والاختبار) + شبكة الأجزاء، ولا تظهر «الاستماع مع المعلم/مراجعة 5/خطة الاستماع» كأقسام مستقلة في القوائم. صفحات القوائم القديمة تعيد التوجيه للمركز، وبقيت صفحات `show/create` كإجراءات تفصيلية (تاريخية/يدوية).
- الخطة `.agents/quran_system_plan.md`: كل جزأين = دفعة (1–2، 3–4، ...، 29–30 = 15 دفعة) بلا أي حالة خاصة لعدد الأجزاء؛ الدفعة k لا تُفتح إلا بعد اجتياز k-1، والحالة تُشتق من الأجزاء المحفوظة فعلياً + مراجعة 5 + آخر اختبار.
- `quran_memorization_batches` (migration `2026_09_16_000005`، و`retake_review_id` في `2026_09_16_000008`): طالب/رقم الدفعة/الجزآن/`status` (`QuranMemorizationBatchStatus`: locked|pending_memorization|pending_review_5|ready_for_test|passed|needs_repeat`) + روابط `plan_id`/`review_5_id`/`retake_review_id`/`last_test_id`. لا صفوف للدفعات المقفلة (تُعرض افتراضية)، والصف يُنشأ عند فتح الدفعة.
- `App\Services\QuranMemorizationGatingService`: `sync()` idempotent (توليد الصفوف، واشتقاق الحالات)، `currentBatch()`، `assertNewTasmeeAllowed()`، `recordCumulativeTest()`، `recordBatchOutcome()`، `createRetakeReview()`، `regenerateReview()`. التوليد التلقائي: عند اكتمال حفظ جزأي الدفعة تُنشأ خطة استماع (8 عناصر «مراجعة 5»، `gate_size=8`) + رأس مراجعة مرتبط عبر `QuranListeningService::createPlan` (يحتاج أستاذاً من دوام الطالب؛ يفشل بهدوء إن تعذّر).
- **الاختبار التراكمي وخمسات الإعادة** (migration `2026_09_16_000008`): اختبار الدفعة k تراكمي على الأجزاء 1..2k وبنتيجة لكل جزء (`recordCumulativeTest`، و`quran_listening_test_items.plan_item_id` صار اختيارياً)؛ `score` = ناجح/الكل ×100 وحد النجاح لقطة على الاختبار. لا يُفتح الاختبار قبل إنهاء **جميع** خمسات الدفعة (`assertCumulativeTestAllowed`، ويُرفض اختبار الخطة لكل-item لخطط الدفعات في `QuranListeningService::recordTest`). الرسوب: يُستخرج `failedJuzNumbers()` من نتيجة الاختبار ويُنشأ تلقائياً رأس «خمسات إعادة رسوب الاختبار» بنوع `QuranKhamsaReviewType::RetakeAfterFail` (٤ خمسات لكل جزء راسب)، مع خياري «إعادة الأجزاء الراسبة فقط» و«إعادة الاختبار كاملًا» (`POST admin|teacher.quran.batches.retake`). اختبار الإعادة يغطي نطاق الإعادة نفسه (`testScopeJuzNumbers`) ولا يُسمح به قبل إنهاء خمسات الإعادة؛ والرسوب مجدداً يولّد مراجعة إعادة جديدة. نجاح = فتح الدفعة التالية تلقائياً + إغلاق الخطة وتثبيت عناصرها وإنهاء الخمسات المتبقية. خيار «مراجعة 5 جديدة» بعد الرسوب: `regenerateReview` يلغي الدورة (بما فيها مراجعة الإعادة) ويعيد توليدها. `quran_khamsa_reviews.type` يميّز النوعين في DB والواجهات (شارة «خمسات إعادة رسوب الاختبار»).
- إعدادات الجامع: `tenant_settings` (migration `2026_09_16_000004`) + `QuranSettingsService` (حد النجاح الافتراضي 80%) وصفحة `/admin/settings/quran` (صلاحيات `quran_settings.view/update`).
- منع التسميع المقفل: تسميع `type=new` خارج نطاق الدفعة الحالية مرفوض في `Admin\QuranTasmeeController` و`Teacher\QuranTasmeeController` (تسميع `revision` حر). شبكة «الأجزاء المحفوظة» الإدارية تبقى مفتوحة (تجاوز إداري) والدفعات تُشتق منها تباعاً.
- **التسميع مدموج في المركز** (migration `2026_09_16_000007` → `quran_recitation_sessions.batch_id`): تسميع «جديد» يُربط تلقائياً بالدفعة التي يقع نطاقه داخلها (`QuranMemorizationGatingService::batchForPageRange`، عبر `QuranJuzMap::juzForPage` + `QuranMemorizationBatch::numberForJuz`)؛ `revision` بلا دفعة. `teacher/admin.quran.tasmee.index` تعيد التوجيه للمركز (مع `student_id`؛ وfallback إلى `quran.index` لمن لا يملك `quran_batch.view`)، وبقيت `create/review/edit` كإجراءات تفصيلية. المركز يعرض دورة مرقّمة: ١. التسميع مع المعلم (تقدّم صفحات الدفعة: `batchMemorizationProgress` = مغطى/متبقي/الصفحة التالية + شبكة صفحات ملوّنة + سجل جلسات الاستماع التفصيلي) ← ٢. مراجعة 5 ← ٣. خطة الاستماع والاختبار. نموذج التسميع يعرض بطاقة الدفعة ويسبق تعبئة النطاق من أول صفحة غير مغطاة، و«ملفي القرآني» للطالب يعرض نفس تقدّم الصفحات والسجل الموحّد. رابط «التسميع» المستقل أُزيل من الـSidebar. `tests/Feature/TasmeeBatchIntegrationTest.php`.
- **«التسميع مع المعلم» — سجل موحّد بطبقة Presenter**: قسم واحد في المركز (و«ملفي القرآني») يجمع جلسات `QuranRecitationSession` (حكم عام: `summary`) وجلسات `QuranReviewSession` (استماع تفصيلي كلمة بكلمة: `detailed`) في Timeline واحد، مع إبقاء الكيانين مستقلين في الـDomain (لا دمج Models ولا تغيير على Coverage/الدفعات — الاستماع دليل إتقان فقط). `App\Services\QuranTeacherTimelineService::forStudent(student, filter, teacherId, limit, reviewShowUrl)` → `Collection<App\Support\QuranTeacherTimelineItem>` (نوع `QuranTeacherTimelineType`: recitation_new|recitation_revision|teacher_listening + `QuranTeacherTimelineDetailLevel`). الفلتر يُدفع داخل كل استعلام (يمنع تجويع مصدر لصالح آخر)، الترتيب حتمي (occurred_at ثم created_at ثم id)، والجلب محدود لكل مصدر قبل الدمج والاقتصاص (20 سجلاً افتراضياً). المركز (`/admin|teacher/quran/batches`) يعرض جلسات الاستماع التفصيلي فقط (`filter=listening` مثبّت بلا أزرار فلترة)، و«ملفي القرآني» للطالب يعرض السجل الموحّد كاملاً. الواجهة: شارات (🟢 جديد/🔵 مراجعة/🟡 استماع) + ملخص تقدم + Progressive Disclosure («عرض الجلسة») عبر `resources/views/components/quran-teacher-timeline.blade.php`. `tests/Feature/QuranTeacherTimelineTest.php` (يشمل اختبار عدم التجويع والترتيب الحتمي والعزل بالأستاذ).
- **بوابة الدخول الموحدة «+ التسميع مع المعلم»**: زر واحد في ترويسة قسم «التسميع مع المعلم» بالمركز (Admin + Teacher) بدل زرّي «+ تسجيل تسميع» و«+ جلسة استماع»، يفتح شاشة اختيار النوع `quran/batches/session-start` (`admin|teacher.quran.batches.session-start`، middleware `permission:quran.tasmee.create,quran_review.create`) عبر `quran/batches/session-type.blade.php`: «تسميع حفظ جديد»/«تسميع مراجعة» → `*.quran.tasmee.create?student_id&type=` (مع prefill للنوع وبقائه عند تبديل الطالب)، و«استماع وتقييم تفصيلي» → `teacher.quran-review.create?student_id` (المدير لا يملك هذا الـWorkflow فلا يظهر له — `Route::has`). التوجيه في `App\Services\QuranTeacherSessionService::typesFor(user, student, routePrefix)` → `QuranTeacherSessionTypeOption` (نوع `QuranTeacherSessionType`) مع فحص `AuthorizationService::can`؛ لا منطق حفظ في الخدمة، والعزل الفعلي للمعلم عبر `QuranScopeService::assertCanManageStudent`، والطالب read-only. اختباراتها في `QuranTeacherTimelineTest` (زر واحد، خيارات المدير/المعلم، 403 خارج النطاق/الأدوار، prefill النوع).
- الشاشات: `/admin/quran/batches` و`/teacher/quran/batches` (شبكة 15 دفعة + زر «مراجعة 5 جديدة»)، و«ملفي القرآني» `/student/quran` (`Student\QuranProfileController`) بشبكة 30 جزءاً (✓/⏳/🔒) ودورة الدفعة الحالية. صلاحيات `quran_batch.view/update` (mosque للمدير، own للأستاذ والطالب للعرض) + backfill `2026_09_16_000006`.
- **تسجيل نتيجة الاختبار بعد المراجعة**: نموذج الاختبار التراكمي في المركز موسوم بـ`id="batch-test"` (صف لكل جزء من أجزاء النطاق)؛ عند اكتمال مراجعة 5 المرتبطة بدفعة ينقل `storeReview` (admin/teacher) مباشرة إلى `…batches.index?student_id=…#batch-test`، والمراجعة الجزئية تبقى في صفحة المراجعة. `review-details` يعرض زر «تسجيل نتيجة الاختبار» (في المركز `#batch-test`، وفي صفحة `show` رابط المركز) للمراجعات المرتبطة بدفعة **بعد اكتمالها فقط**، وبطاقة «٣. الاختبار التراكمي» تربط إليه عند `ready_for_test`. بعد الرسوب يعرض المركز «رسب في الأجزاء: …» وزرّي إعادة الأجزاء الراسبة/إعادة كاملًا، وقسم «خمسات إعادة رسوب الاختبار» مميّزاً، ولا يُفتح الاختبار حتى إنهاء الإعادة. `tests/Feature/MemorizationBatchTest.php` (الانتقال، بوابات الرفض، مسار الإعادة الكامل، والشاشات).
- نجاح الدفعة الأخيرة (29–30) ينشئ `QuranCompletion` بحالة pending تلقائياً (`QuranProgramService::recordCompletion`) مع إشعار للإدارة → التأكيد المعتاد يشغّل Hafiz ثم البرنامج التأهيلي.
- `tests/Feature/MemorizationBatchTest.php`: الديناميكية (26 جزءاً = الدفعة 13)، الاختبار التراكمي (نطاق 1..2k، الدرجة، لقطة حد النجاح)، بوابات «لا اختبار قبل إنهاء الخمسات»، مسار الرسوب الكامل (استخراج الأجزاء الراسبة → خمسات إعادة تلقائية → خيار كامل/راسب فقط → اختبار الإعادة → فتح التالية)، رفض التسميع المقفل، فتح طلب الإتمام بعد الدفعة 15، والشاشات.

### Route structure
| File / prefix | Auth | Purpose |
|------|------|---------|
| `routes/web.php` → `/admin/*` | `role:admin` | Mosque manager Blade UI (students/teachers/classes/schedules/attendance/exams/grades/reports/announcements/quran/reward-points/users) |
| `routes/web.php` → `/teacher/*` | `role:teacher` | Teacher Blade UI |
| `routes/web.php` → `/super-admin/*` | `role:super_admin` | Central management: mosques CRUD, per-mosque users + roles + permission matrix, enter-mosque context |
| `routes/api.php` → `/api/v1/*` | `auth:sanctum` + `tenant` | Mobile API (login/logout/me, admin & teacher areas) |

No public register (removed). Login only. Unauthenticated endpoints: `POST /api/v1/login`.

### Controllers
- `app/Http/Controllers/Admin/`, `Teacher/`, `SuperAdmin/` — Blade controllers (sub-`Actions/` for shared ops).
- `app/Http/Controllers/Api/V1/{Admin,Teacher}` — REST (extends `Api/BaseApiController`, teachers extend `Api/V1/Teacher/BaseTeacherController`).
- `app/Http/Controllers/Api/Admin|Teacher` (no V1) were removed — do not reintroduce.

### Key models (UUID PK + multi-tenant unless noted)
`User` (tenant_id nullable), `Tenant` (unscoped), `Role`, `Permission` (unscoped), `Student`, `Teacher`, `Classroom`, `Section`, `Subject`, `Schedule`, `Attendance`, `Exam`, `Grade`, `Homework`, `HomeworkSubmission`, `Lesson`, `Announcement`, `Message`, `QuranSurah`/`QuranAyah` (unscoped), `QuranReviewSession`, `QuranReviewWord`, `RewardPoint`, `TeacherRating`, `TeacherCertificate`, `StudentJuzMemorization`, `QuranKhamsaReview`, `QuranKhamsaReviewItem`, `QuranListeningPlan`/`QuranListeningPlanItem`/`QuranListeningTest`/`QuranListeningTestItem`, `QuranMemorizationBatch`, `TenantSetting`.

### Grade/schedule workflow
- Grades: draft → submitted → approved (teacher `grades.submit`, admin approve). Grades cannot be edited once approved (enforced in `SaveGradesAction`).
- Schedules: no approval workflow yet (spec §14 pending).

### Attendance
- Session-based source of truth (`attendance_sessions` + `attendance_records`, statuses present/late/absent/excused). The sidebar "الحضور والغياب والتأخير" opens `admin.attendance.summary`: a per-student table (حاضر/غائب/متأخر/معذور + percentage via `AttendanceMetricService::studentStats`) filterable by date range and section. Daily sessions stay at `admin.attendance.index` and the per-session grid at `admin.attendance.history`.
- مدير الجوامع can jump straight in: `POST super-admin.mosques.enter` accepts `to=attendance` (button on the central dashboard + mosques list) and lands on the summary inside the mosque context. `tests/Feature/AttendanceSessionsTest.php` covers the page for both the manager and the super admin.

## Database
- SQLite default (`database/database.sqlite`); MySQL supported. SESSION/QUEUE = database; dev runs `queue:listen`.
- ~26 migration files, incl. RBAC (`2026_09_05_00000*`) and tenant management fields (`code/email/status/logo`).

## Testing
- PHPUnit 11.5, SQLite `:memory:`; `composer test` or `php artisan test --filter=TestName`.
- `tests/Feature/`: `AuthTest`, `StudentApiTest`, `DashboardApiTest`, `Phase0RegressionTest` (bug-fix regressions), `AuthorizationTest` (RBAC + isolation + super-admin enter/exit). New spec features should add tests here.
- TestCase resets tenant config in `tearDown`.

## Code style
- Laravel Pint (`vendor/bin/pint --dirty`), 4-space indent, LF, UTF-8. No CI workflows.

## Project docs
- `.agents/mosque_management_implementation.md` — the Arabic implementation spec; Definition of Done in §47. Current status vs spec: foundation + single-mosque academic core + RBAC/super-admin (central dashboard, mosques/users/roles + permission matrix, per-user overrides inside the user form) + schedule specializations/programs (تخصصات الجداول: `programs`/`program_periods`/`program_attributes`, schedule program+period+shift linkage, per-shift program access, dynamic attribute options from students with filters via `options_source`/`options_config`, spec `.agents/mosque_management_schedule_programs.md`) + teacher payroll/رواتب المعلمين (monthly salary, monthly work-hour totals, payment resets the hours counter, teacher deposit-only page + notification) + per-shift classrooms/sections (each دوام has its own صفوف وشعب, cascade on rebinding, schedule shift consistency) + نقاط المكافآت لكل دوام (قواعد قابلة للتعديل لكل دوام: حفظ صفحات جديدة تراكمي مع ترحيل الباقي / إتمام خمسة / اجتياز اختبار الدفعة، منح تلقائي محمي من التكرار وغير قابل للحذف من الأستاذ، وبوابة الطالب «نقاطي») + «مراجعة 5» (خمسات: كل جزء ٤ خمسات × ٥ صفحات، قفل الأجزاء غير المحفوظة، تخصيص بأستاذ ودوام، بوابة الطالب، ومراجعة جماعية بمربعات وتحديد متتالي مع تسجيل أخطاء كلمة بكلمة وإنشاء جلسة استماع تُنهي الخمسات) + «خطة الاستماع والاختبار» (عناصر «جديد» بنطاق صفحات داخل حدود الجزء + عناصر «مراجعة 5» مدموجة بمزامنة اتجاهين مع شاشات الخمسات، بناء من الأجزاء المحفوظة مع توليد تلقائي، فتح بالدفعات حتى ١٠، اختبار يفتح ما بعده، ربط جلسات «الاستماع مع المعلم» عند الاستماع والاختبار، تلاوة CDN لكل آية، بوابة الطالب) + «دفعات الحفظ» (كل جزأين دفعة ديناميكياً بلا حالات خاصة، توليد مراجعة 5 وخطة استماع تلقائياً عند اكتمال الجزأين، اختبار تراكمي بنتيجة لكل جزء من 1..2k ودرجة محسوبة من الأجزاء وحد نجاح لكل جامع مع لقطة على كل اختبار، منع تسميع الأجزاء المقفلة على الـBackend، خمسات إعادة رسوب الاختبار تلقائياً للأجزاء الراسبة مع خياري إعادة الراسب فقط/إعادة كاملًا واختبار إعادة بنطاق الإعادة، إعادة توليد المراجعة عند الرسوب، ملف قرآني موحد للطالب، وفتح طلب إتمام الحفظ بعد الدفعة 15) + دمج واجهات القرآن الأربع («الاستماع مع المعلم» و«مراجعة 5» و«خطة الاستماع» و«دفعات الحفظ») في مركز واحد: `/admin/quran/batches` و`/teacher/quran/batches` و«ملفي القرآني» `/student/quran`، وصفحات القوائم القديمة تعيد التوجيه للمركز + بوابة دخول موحدة «+ التسميع مع المعلم» (شاشة اختيار النوع: حفظ جديد/مراجعة/استماع تفصيلي مع prefill) + «الدورات الشرعية» (إنشاء مركزي من مدير الجوامع: اختيار الجامع والمشرفين المتعددين والطلاب الموجودين أو الجدد، إشعار مدير الجامع، وربط `student_id` وحالة الحفظ لكل طالب مسجَّل) done. **Not yet built**: schedule approval workflow, grade reject, student transfer, soft deletes, custom fields + field-level permissions, audit logs, notifications, reports export, schedule/grade full 5-state engine, per-spec test matrix for those features, واجهات API لخطة الاستماع ودفعات الحفظ.
- Removed legacy: dental-clinic modules (Lab/Supplier/Center), pre-V1 API controllers, public registration.
